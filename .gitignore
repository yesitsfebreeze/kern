/target
Cargo.lock

# kern runtime state (keep kern.toml tracked)
/.relay/kern
/.relay/capture
/.relay/journal
/.relay/*.log

# local tooling / vaults (not part of the product)
/.claude
/.obsidian
/vicky
NUL
