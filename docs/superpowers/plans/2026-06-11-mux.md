# Mux PTY Multiplexer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `kern mux` as the default no-args launch mode — a crossterm split-pane PTY TUI that manages multiple `claude` (or configurable) agent processes with an MCP server exposing four tools (`mux_spawn`, `mux_send`, `mux_list`, `mux_status`).

**Architecture:** `portable-pty` + `vt100` provide cross-platform PTY spawning and in-memory screen models; `crossterm` renders them into a two-column split layout. A background tokio task serves the four mux MCP tools over a TCP loopback socket so agents inside panes can spawn and communicate with sibling panes programmatically. `kern --daemon` / `kern daemon` keep the existing knowledge substrate path untouched.

**Tech Stack:** Rust, `portable-pty 0.8`, `vt100 0.15`, `crossterm 0.29` (workspace), `tokio` (workspace), `trnsprt` (local), `journal` (local), `serde_json` (workspace)

**Spec:** `docs/superpowers/specs/2026-06-11-mux-design.md`

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `src/config/mux.rs` | `MuxConfig`, `KeyMap`, `parse_key_event` |
| Modify | `src/config/mod.rs` | add `mod mux`, re-export, add `pub mux: MuxConfig` to `Config` |
| Create | `src/mux/pty.rs` | `PtySession` — spawn/drain/write/resize/screen_text |
| Create | `src/mux/registry.rs` | `PaneRegistry`, `SharedRegistry` type alias |
| Create | `src/mux/tui.rs` | `Guard`, `run_tui()`, `draw_frame()`, key→bytes conversion |
| Create | `src/mux/mcp.rs` | `MuxMcpServer`, four tool schemas + handlers |
| Create | `src/mux/mod.rs` | `pub async fn run_mux(cfg)` — top-level entry, wires everything |
| Modify | `src/lib.rs` | add `pub mod mux;` |
| Modify | `src/commands.rs` | add `Commands::Daemon` variant + dispatch arm |
| Modify | `src/main.rs` | update match to call `run_mux` on no-args |
| Modify | `Cargo.toml` | add `portable-pty = "0.8"`, `vt100 = "0.15"` |

---

## Task 1: Dependencies + MuxConfig

**Files:**
- Modify: `Cargo.toml`
- Create: `src/config/mux.rs`
- Modify: `src/config/mod.rs`

- [ ] **Step 1: Add dependencies to Cargo.toml**

In the `[dependencies]` section of `kern/Cargo.toml`, add immediately after the `heed` line:

```toml
portable-pty = "0.8"
vt100 = "0.15"
```

- [ ] **Step 2: Verify deps resolve**

```
cargo fetch
```

Expected: no errors (crates download successfully).

- [ ] **Step 3: Write the failing config test**

Create `src/config/mux.rs` with just the test:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn mux_config_defaults_are_correct() {
        let c = MuxConfig::default();
        assert_eq!(c.agent_cmd, "claude");
        assert_eq!(c.key_new_pane, "alt+n");
        assert_eq!(c.key_close_pane, "ctrl+w");
        assert_eq!(c.key_cycle, "tab");
        assert_eq!(c.key_quit, "ctrl+q");
        assert_eq!(c.mcp_addr, "127.0.0.1:7779");
    }

    #[test]
    fn parse_key_event_tab() {
        use crossterm::event::{KeyCode, KeyModifiers};
        let ev = parse_key_event("tab").unwrap();
        assert_eq!(ev.code, KeyCode::Tab);
        assert_eq!(ev.modifiers, KeyModifiers::NONE);
    }

    #[test]
    fn parse_key_event_alt_n() {
        use crossterm::event::{KeyCode, KeyModifiers};
        let ev = parse_key_event("alt+n").unwrap();
        assert_eq!(ev.code, KeyCode::Char('n'));
        assert!(ev.modifiers.contains(KeyModifiers::ALT));
    }

    #[test]
    fn parse_key_event_ctrl_w() {
        use crossterm::event::{KeyCode, KeyModifiers};
        let ev = parse_key_event("ctrl+w").unwrap();
        assert_eq!(ev.code, KeyCode::Char('w'));
        assert!(ev.modifiers.contains(KeyModifiers::CONTROL));
    }

    #[test]
    fn parse_key_event_unknown_returns_none() {
        assert!(parse_key_event("meta+z").is_none());
    }

    #[test]
    fn keymap_matches_defaults() {
        use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
        let km = KeyMap::from_config(&MuxConfig::default());
        assert!(km.matches_cycle(&KeyEvent::new(KeyCode::Tab, KeyModifiers::NONE)));
        assert!(km.matches_new_pane(&KeyEvent::new(KeyCode::Char('n'), KeyModifiers::ALT)));
        assert!(km.matches_close_pane(&KeyEvent::new(KeyCode::Char('w'), KeyModifiers::CONTROL)));
        assert!(km.matches_quit(&KeyEvent::new(KeyCode::Char('q'), KeyModifiers::CONTROL)));
    }
}
```

- [ ] **Step 4: Run test — verify it fails (struct not defined)**

```
cargo test -p kern config::mux 2>&1 | head -20
```

Expected: compile error mentioning `MuxConfig` not found.

- [ ] **Step 5: Implement `src/config/mux.rs`**

Replace the file with the full implementation:

```rust
use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
use serde::{Deserialize, Serialize};

/// Configuration for `kern mux` — the default PTY-multiplexer TUI mode.
///
/// All fields have safe defaults so an empty `[mux]` section (or no section
/// at all) is valid.
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(default)]
pub struct MuxConfig {
    /// Command spawned for each agent pane. Defaults to `claude`.
    pub agent_cmd: String,
    /// Key binding to spawn a new sub-pane. Default `alt+n`.
    pub key_new_pane: String,
    /// Key binding to close the focused sub-pane. Default `ctrl+w`.
    pub key_close_pane: String,
    /// Key binding to cycle focus between panes. Default `tab`.
    pub key_cycle: String,
    /// Key binding to quit mux (kills all panes). Default `ctrl+q`.
    pub key_quit: String,
    /// TCP address the mux MCP server listens on. Default `127.0.0.1:7779`.
    pub mcp_addr: String,
}

impl Default for MuxConfig {
    fn default() -> Self {
        Self {
            agent_cmd:     "claude".into(),
            key_new_pane:  "alt+n".into(),
            key_close_pane: "ctrl+w".into(),
            key_cycle:     "tab".into(),
            key_quit:      "ctrl+q".into(),
            mcp_addr:      "127.0.0.1:7779".into(),
        }
    }
}

/// Parsed keybindings built once at startup from [`MuxConfig`].
/// Each field is the crossterm [`KeyEvent`] that triggers the action.
pub struct KeyMap {
    pub new_pane:   KeyEvent,
    pub close_pane: KeyEvent,
    pub cycle:      KeyEvent,
    pub quit:       KeyEvent,
}

impl KeyMap {
    /// Build a `KeyMap` from config, falling back to hard-coded defaults
    /// for any key that fails to parse.
    pub fn from_config(cfg: &MuxConfig) -> Self {
        Self {
            new_pane:   parse_key_event(&cfg.key_new_pane)
                .unwrap_or_else(|| KeyEvent::new(KeyCode::Char('n'), KeyModifiers::ALT)),
            close_pane: parse_key_event(&cfg.key_close_pane)
                .unwrap_or_else(|| KeyEvent::new(KeyCode::Char('w'), KeyModifiers::CONTROL)),
            cycle:      parse_key_event(&cfg.key_cycle)
                .unwrap_or_else(|| KeyEvent::new(KeyCode::Tab, KeyModifiers::NONE)),
            quit:       parse_key_event(&cfg.key_quit)
                .unwrap_or_else(|| KeyEvent::new(KeyCode::Char('q'), KeyModifiers::CONTROL)),
        }
    }

    pub fn matches_new_pane(&self, ev: &KeyEvent) -> bool   { self.new_pane == *ev }
    pub fn matches_close_pane(&self, ev: &KeyEvent) -> bool { self.close_pane == *ev }
    pub fn matches_cycle(&self, ev: &KeyEvent) -> bool      { self.cycle == *ev }
    pub fn matches_quit(&self, ev: &KeyEvent) -> bool       { self.quit == *ev }
}

/// Parse a key-binding string like `"alt+n"`, `"ctrl+w"`, or `"tab"` into a
/// crossterm [`KeyEvent`]. Returns `None` for unrecognised modifiers or keys.
pub fn parse_key_event(s: &str) -> Option<KeyEvent> {
    let s = s.to_lowercase();
    let parts: Vec<&str> = s.split('+').collect();
    let (mod_parts, key_part) = parts.split_at(parts.len().saturating_sub(1));
    let key_str = key_part.first().copied().unwrap_or("");

    let mut mods = KeyModifiers::NONE;
    for m in mod_parts {
        match *m {
            "ctrl"  => mods |= KeyModifiers::CONTROL,
            "alt"   => mods |= KeyModifiers::ALT,
            "shift" => mods |= KeyModifiers::SHIFT,
            _       => return None,
        }
    }

    let code = match key_str {
        "tab"       => KeyCode::Tab,
        "enter"     => KeyCode::Enter,
        "esc"       => KeyCode::Esc,
        "backspace" => KeyCode::Backspace,
        "delete"    => KeyCode::Delete,
        "up"        => KeyCode::Up,
        "down"      => KeyCode::Down,
        "left"      => KeyCode::Left,
        "right"     => KeyCode::Right,
        "home"      => KeyCode::Home,
        "end"       => KeyCode::End,
        c if c.chars().count() == 1 => KeyCode::Char(c.chars().next()?),
        _ => return None,
    };

    Some(KeyEvent::new(code, mods))
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn mux_config_defaults_are_correct() {
        let c = MuxConfig::default();
        assert_eq!(c.agent_cmd, "claude");
        assert_eq!(c.key_new_pane, "alt+n");
        assert_eq!(c.key_close_pane, "ctrl+w");
        assert_eq!(c.key_cycle, "tab");
        assert_eq!(c.key_quit, "ctrl+q");
        assert_eq!(c.mcp_addr, "127.0.0.1:7779");
    }

    #[test]
    fn parse_key_event_tab() {
        let ev = parse_key_event("tab").unwrap();
        assert_eq!(ev.code, KeyCode::Tab);
        assert_eq!(ev.modifiers, KeyModifiers::NONE);
    }

    #[test]
    fn parse_key_event_alt_n() {
        let ev = parse_key_event("alt+n").unwrap();
        assert_eq!(ev.code, KeyCode::Char('n'));
        assert!(ev.modifiers.contains(KeyModifiers::ALT));
    }

    #[test]
    fn parse_key_event_ctrl_w() {
        let ev = parse_key_event("ctrl+w").unwrap();
        assert_eq!(ev.code, KeyCode::Char('w'));
        assert!(ev.modifiers.contains(KeyModifiers::CONTROL));
    }

    #[test]
    fn parse_key_event_unknown_returns_none() {
        assert!(parse_key_event("meta+z").is_none());
    }

    #[test]
    fn keymap_matches_defaults() {
        let km = KeyMap::from_config(&MuxConfig::default());
        assert!(km.matches_cycle(&KeyEvent::new(KeyCode::Tab, KeyModifiers::NONE)));
        assert!(km.matches_new_pane(&KeyEvent::new(KeyCode::Char('n'), KeyModifiers::ALT)));
        assert!(km.matches_close_pane(&KeyEvent::new(KeyCode::Char('w'), KeyModifiers::CONTROL)));
        assert!(km.matches_quit(&KeyEvent::new(KeyCode::Char('q'), KeyModifiers::CONTROL)));
    }
}
```

- [ ] **Step 6: Wire MuxConfig into Config**

In `src/config/mod.rs`:

Add `mod mux;` alongside the other `mod` declarations at the top.

Add to the re-exports:
```rust
pub use mux::{KeyMap, MuxConfig, parse_key_event};
```

Add field to `Config` struct (after `pub journal: JournalConfig,`):
```rust
pub mux: MuxConfig,
```

- [ ] **Step 7: Run config tests — verify they pass**

```
cargo test -p kern config::mux 2>&1 | tail -15
```

Expected: `test result: ok. 6 passed; 0 failed`.

- [ ] **Step 8: Verify full crate still compiles**

```
cargo check -p kern 2>&1 | tail -10
```

Expected: `Finished` with no errors (only possible warnings).

- [ ] **Step 9: Commit**

```
git add Cargo.toml Cargo.lock src/config/mux.rs src/config/mod.rs
git commit -m "feat(mux): add MuxConfig + KeyMap with parse_key_event"
```

---

## Task 2: PtySession

**Files:**
- Create: `src/mux/pty.rs`

> **Context:** `portable_pty::native_pty_system()` returns a system PTY factory. Call `openpty(PtySize)` → `PtyPair { master, slave }`, then `slave.spawn_command(cmd)` → child, `master.take_writer()` → writer, `master.try_clone_reader()` → reader. Drop `slave` after spawning. A background `std::thread` drains the reader into an `mpsc::channel`. See `relay/src/editor_process.rs::EditorProcess::spawn` for the exact pattern.

- [ ] **Step 1: Write the failing tests**

Create `src/mux/pty.rs` with just tests (will fail at compile since struct not defined yet):

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn screen_text_from_empty_parser() {
        // A fresh vt100 parser with no output → screen_text is empty
        let parser = vt100::Parser::new(5, 10, 0);
        let text = screen_text_from(parser.screen());
        assert!(
            text.trim().is_empty(),
            "fresh parser produces blank screen: got {text:?}"
        );
    }

    #[test]
    fn screen_text_trims_trailing_whitespace_per_row() {
        // Feed some content and verify trailing spaces are stripped
        let mut parser = vt100::Parser::new(3, 10, 0);
        parser.process(b"hi");
        let text = screen_text_from(parser.screen());
        let first_line = text.lines().next().unwrap_or("");
        assert_eq!(first_line, "hi", "trailing spaces stripped: got {first_line:?}");
    }

    #[test]
    fn key_event_to_bytes_char() {
        use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
        let ev = KeyEvent::new(KeyCode::Char('a'), KeyModifiers::NONE);
        assert_eq!(key_event_to_bytes(&ev), Some(b"a".to_vec()));
    }

    #[test]
    fn key_event_to_bytes_ctrl_c() {
        use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
        let ev = KeyEvent::new(KeyCode::Char('c'), KeyModifiers::CONTROL);
        // Ctrl+C = 0x03
        assert_eq!(key_event_to_bytes(&ev), Some(vec![0x03]));
    }

    #[test]
    fn key_event_to_bytes_enter() {
        use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
        let ev = KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE);
        assert_eq!(key_event_to_bytes(&ev), Some(b"\r".to_vec()));
    }

    #[test]
    fn key_event_to_bytes_up_arrow() {
        use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
        let ev = KeyEvent::new(KeyCode::Up, KeyModifiers::NONE);
        assert_eq!(key_event_to_bytes(&ev), Some(b"\x1b[A".to_vec()));
    }
}
```

- [ ] **Step 2: Run test — verify compile fails**

```
cargo test -p kern mux::pty 2>&1 | head -10
```

Expected: compile error (module not found or struct undefined).

- [ ] **Step 3: Implement `src/mux/pty.rs`**

```rust
//! PTY session — one OS PTY pair + spawned process + vt100 screen model.
//!
//! Cross-platform via `portable_pty`: uses ConPTY on Windows and openpty
//! on Unix/WSL with no `#[cfg]` walls in this file.

use std::io::Write;
use std::sync::mpsc;

use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
use portable_pty::{CommandBuilder, MasterPty, native_pty_system, PtySize};

pub struct PtySession {
    /// Short random id — 8 lowercase hex chars generated at spawn time.
    pub id: String,
    /// Human label shown in the tab strip (e.g. `"main"`, `"sub-1"`).
    pub label: String,
    /// The command that was spawned (e.g. `"claude"`).
    pub cmd: String,
    master: Box<dyn MasterPty + Send>,
    writer: Box<dyn Write + Send>,
    /// In-memory VT100 screen model updated by `drain()`.
    pub parser: vt100::Parser,
    rx: mpsc::Receiver<Vec<u8>>,
    child: Box<dyn portable_pty::Child + Send + Sync>,
    /// Set to `true` once the child process has exited.
    pub exited: bool,
}

impl PtySession {
    /// Spawn a new PTY session running `cmd`.
    ///
    /// Launches a reader thread that drains PTY output into an mpsc channel;
    /// call [`drain`] each frame to feed the bytes into the vt100 parser.
    pub fn spawn(id: String, label: String, cmd: String, cols: u16, rows: u16) -> anyhow::Result<Self> {
        let pty_system = native_pty_system();
        let pair = pty_system.openpty(PtySize { rows, cols, pixel_width: 0, pixel_height: 0 })?;

        let slave  = pair.slave;
        let master = pair.master;

        let mut builder = CommandBuilder::new(&cmd);
        // Inherit environment so PATH, HOME, etc. reach the agent.
        builder.env("TERM", "xterm-256color");
        let child = slave.spawn_command(builder)?;
        drop(slave);

        let writer = master.take_writer()?;
        let mut reader = master.try_clone_reader()?;

        let (tx, rx) = mpsc::channel::<Vec<u8>>();
        std::thread::spawn(move || {
            let mut buf = [0u8; 4096];
            loop {
                match std::io::Read::read(&mut reader, &mut buf) {
                    Ok(0) | Err(_) => break,
                    Ok(n) => {
                        if tx.send(buf[..n].to_vec()).is_err() {
                            break;
                        }
                    }
                }
            }
        });

        let parser = vt100::Parser::new(rows, cols, 0);

        Ok(Self { id, label, cmd, master, writer, parser, rx, child, exited: false })
    }

    /// Drain pending PTY output bytes into the vt100 parser. Call each frame.
    pub fn drain(&mut self) {
        while let Ok(bytes) = self.rx.try_recv() {
            self.parser.process(&bytes);
        }
    }

    /// Write raw bytes to the PTY stdin (keystrokes or MCP-driven text).
    pub fn write_input(&mut self, bytes: &[u8]) {
        let _ = self.writer.write_all(bytes);
        let _ = self.writer.flush();
    }

    /// Resize the PTY and rebuild the vt100 parser at the new dimensions.
    /// Rebuilding the parser resets the in-memory screen — acceptable for resize.
    pub fn resize(&mut self, cols: u16, rows: u16) {
        let _ = self.master.resize(PtySize { rows, cols, pixel_width: 0, pixel_height: 0 });
        self.parser = vt100::Parser::new(rows, cols, 0);
    }

    /// Poll whether the child process has exited. Sets `self.exited` once true.
    pub fn poll_exited(&mut self) -> bool {
        if self.exited { return true; }
        if matches!(self.child.try_wait(), Ok(Some(_))) {
            self.exited = true;
        }
        self.exited
    }

    /// Return the visible screen contents as plain text: each row on its own
    /// line, trailing whitespace trimmed, blank trailing rows omitted.
    pub fn screen_text(&self) -> String {
        screen_text_from(self.parser.screen())
    }

    /// Kill the child process (best-effort).
    pub fn kill(&mut self) {
        let _ = self.child.kill();
    }
}

impl Drop for PtySession {
    fn drop(&mut self) {
        self.kill();
    }
}

/// Extract plain text from a `vt100::Screen` — shared by `PtySession::screen_text`
/// and the unit tests (which build a parser directly without spawning a process).
pub fn screen_text_from(screen: &vt100::Screen) -> String {
    let (rows, cols) = screen.size();
    let mut lines: Vec<String> = Vec::with_capacity(rows as usize);
    for row in 0..rows {
        let mut line = String::with_capacity(cols as usize);
        for col in 0..cols {
            match screen.cell(row, col) {
                Some(cell) => {
                    let c = cell.contents();
                    if c.is_empty() { line.push(' '); } else { line.push_str(c); }
                }
                None => line.push(' '),
            }
        }
        lines.push(line.trim_end().to_string());
    }
    // Drop trailing blank lines.
    while lines.last().map(|l: &String| l.is_empty()).unwrap_or(false) {
        lines.pop();
    }
    lines.join("\n")
}

/// Convert a crossterm [`KeyEvent`] to the raw byte sequence the PTY expects.
/// Returns `None` for key combinations we don't handle (e.g. F-keys).
pub fn key_event_to_bytes(ev: &KeyEvent) -> Option<Vec<u8>> {
    let ctrl = ev.modifiers.contains(KeyModifiers::CONTROL);
    match ev.code {
        KeyCode::Char(c) => {
            if ctrl {
                // Ctrl+A = 0x01 … Ctrl+Z = 0x1A
                let lower = c.to_ascii_lowercase();
                if lower.is_ascii_alphabetic() {
                    return Some(vec![lower as u8 - b'a' + 1]);
                }
                None
            } else {
                let mut buf = [0u8; 4];
                Some(c.encode_utf8(&mut buf).as_bytes().to_vec())
            }
        }
        KeyCode::Enter     => Some(b"\r".to_vec()),
        KeyCode::Backspace => Some(b"\x7f".to_vec()),
        KeyCode::Esc       => Some(b"\x1b".to_vec()),
        KeyCode::Tab       => Some(b"\t".to_vec()),
        KeyCode::Up        => Some(b"\x1b[A".to_vec()),
        KeyCode::Down      => Some(b"\x1b[B".to_vec()),
        KeyCode::Right     => Some(b"\x1b[C".to_vec()),
        KeyCode::Left      => Some(b"\x1b[D".to_vec()),
        KeyCode::Home      => Some(b"\x1b[H".to_vec()),
        KeyCode::End       => Some(b"\x1b[F".to_vec()),
        KeyCode::Delete    => Some(b"\x1b[3~".to_vec()),
        _ => None,
    }
}

/// Generate an 8-character lowercase hex session id using the `rand` workspace dep.
pub fn new_session_id() -> String {
    use rand::Rng;
    let mut rng = rand::rng();
    format!("{:08x}", rng.random::<u32>())
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn screen_text_from_empty_parser() {
        let parser = vt100::Parser::new(5, 10, 0);
        let text = screen_text_from(parser.screen());
        assert!(text.trim().is_empty(), "fresh parser: got {text:?}");
    }

    #[test]
    fn screen_text_trims_trailing_whitespace_per_row() {
        let mut parser = vt100::Parser::new(3, 10, 0);
        parser.process(b"hi");
        let text = screen_text_from(parser.screen());
        let first = text.lines().next().unwrap_or("");
        assert_eq!(first, "hi", "trailing spaces stripped: got {first:?}");
    }

    #[test]
    fn key_event_to_bytes_char() {
        let ev = KeyEvent::new(KeyCode::Char('a'), KeyModifiers::NONE);
        assert_eq!(key_event_to_bytes(&ev), Some(b"a".to_vec()));
    }

    #[test]
    fn key_event_to_bytes_ctrl_c() {
        let ev = KeyEvent::new(KeyCode::Char('c'), KeyModifiers::CONTROL);
        assert_eq!(key_event_to_bytes(&ev), Some(vec![0x03]));
    }

    #[test]
    fn key_event_to_bytes_enter() {
        let ev = KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE);
        assert_eq!(key_event_to_bytes(&ev), Some(b"\r".to_vec()));
    }

    #[test]
    fn key_event_to_bytes_up_arrow() {
        let ev = KeyEvent::new(KeyCode::Up, KeyModifiers::NONE);
        assert_eq!(key_event_to_bytes(&ev), Some(b"\x1b[A".to_vec()));
    }

    #[test]
    fn new_session_id_is_8_hex_chars() {
        let id = new_session_id();
        assert_eq!(id.len(), 8);
        assert!(id.chars().all(|c| c.is_ascii_hexdigit()), "not hex: {id}");
    }
}
```

> **Note:** `portable_pty::Child` requires `Send + Sync` for the field type. If the crate's trait is only `Send`, change the field to `Box<dyn portable_pty::Child + Send>` (without `+ Sync`) — this compiles fine because we never share the child across threads.

- [ ] **Step 4: Create stub `src/mux/mod.rs`** (needed so `mod mux` in lib.rs compiles)

```rust
pub mod mcp;
pub mod pty;
pub mod registry;
pub mod tui;

pub use pty::{PtySession, new_session_id};

/// Launch the mux PTY-multiplexer TUI. Called when `kern` is run with no
/// subcommand and `--daemon` is not set. Placeholder until Task 6.
pub async fn run_mux(_cfg: &crate::config::Config) {
    eprintln!("kern: mux TUI not yet implemented — use kern --daemon for the knowledge substrate");
}
```

- [ ] **Step 5: Add `pub mod mux;` to `src/lib.rs`**

Add after the existing `pub mod viewer;` line:

```rust
/// PTY-multiplexer TUI mode — the default `kern` launch mode.
pub mod mux;
```

- [ ] **Step 6: Run pty tests**

```
cargo test -p kern mux::pty 2>&1 | tail -15
```

Expected: `test result: ok. 7 passed; 0 failed`.

- [ ] **Step 7: Commit**

```
git add src/mux/pty.rs src/mux/mod.rs src/lib.rs
git commit -m "feat(mux): add PtySession, screen_text_from, key_event_to_bytes"
```

---

## Task 3: PaneRegistry

**Files:**
- Create: `src/mux/registry.rs`
- Modify: `src/mux/mod.rs` (add re-export)

- [ ] **Step 1: Write the failing tests**

Create `src/mux/registry.rs` with tests only:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn make_registry() -> PaneRegistry {
        // Use "echo" as a fast-exiting command so tests don't hang.
        // On Windows this is "cmd /C echo hi"; on Unix "sh -c 'echo hi'".
        #[cfg(windows)]
        let cmd = "cmd";
        #[cfg(not(windows))]
        let cmd = "sh";
        PaneRegistry::new(cmd.to_string(), 80, 24).expect("spawn echo pane")
    }

    #[test]
    fn new_creates_main_pane() {
        let reg = make_registry();
        assert_eq!(reg.panes.len(), 1);
        assert_eq!(reg.panes[0].label, "main");
        assert_eq!(reg.focus, 0);
    }

    #[test]
    fn spawn_pane_returns_id_and_appends() {
        let mut reg = make_registry();
        #[cfg(windows)]
        let cmd = "cmd";
        #[cfg(not(windows))]
        let cmd = "sh";
        let id = reg.spawn_pane("sub-1".to_string(), cmd.to_string(), 80, 24).expect("spawn");
        assert_eq!(id.len(), 8, "id is 8 hex chars");
        assert_eq!(reg.panes.len(), 2);
        assert_eq!(reg.panes[1].label, "sub-1");
    }

    #[test]
    fn cycle_focus_wraps() {
        let mut reg = make_registry();
        #[cfg(windows)]
        let cmd = "cmd";
        #[cfg(not(windows))]
        let cmd = "sh";
        reg.spawn_pane("sub-1".to_string(), cmd.to_string(), 80, 24).unwrap();
        reg.spawn_pane("sub-2".to_string(), cmd.to_string(), 80, 24).unwrap();
        assert_eq!(reg.focus, 0);
        reg.cycle_focus(); assert_eq!(reg.focus, 1);
        reg.cycle_focus(); assert_eq!(reg.focus, 2);
        reg.cycle_focus(); assert_eq!(reg.focus, 0); // wraps
    }

    #[test]
    fn find_by_id() {
        let mut reg = make_registry();
        #[cfg(windows)]
        let cmd = "cmd";
        #[cfg(not(windows))]
        let cmd = "sh";
        let id = reg.spawn_pane("sub-1".to_string(), cmd.to_string(), 80, 24).unwrap();
        assert!(reg.find(&id).is_some());
        assert!(reg.find("nonexistent").is_none());
    }
}
```

- [ ] **Step 2: Run test — verify compile fails (struct not defined)**

```
cargo test -p kern mux::registry 2>&1 | head -10
```

- [ ] **Step 3: Implement `src/mux/registry.rs`**

```rust
//! Registry of active PTY panes.
//!
//! `PaneRegistry` owns the `Vec<PtySession>` and the focus index.
//! Wrap in `SharedRegistry` (`Arc<Mutex<PaneRegistry>>`) to share between
//! the TUI render loop and the MCP server task.

use std::sync::{Arc, Mutex};

use crate::mux::pty::{new_session_id, PtySession};

pub type SharedRegistry = Arc<Mutex<PaneRegistry>>;

pub struct PaneRegistry {
    pub panes: Vec<PtySession>,
    /// Index of the currently focused pane. 0 = main.
    pub focus: usize,
    /// Terminal dimensions at the time of last resize.
    pub cols: u16,
    pub rows: u16,
}

impl PaneRegistry {
    /// Create a new registry and immediately spawn the main pane.
    pub fn new(main_cmd: String, cols: u16, rows: u16) -> anyhow::Result<Self> {
        let id    = new_session_id();
        let pane  = PtySession::spawn(id, "main".to_string(), main_cmd, cols, rows)?;
        Ok(Self { panes: vec![pane], focus: 0, cols, rows })
    }

    /// Spawn a new sub-pane and return its session id.
    /// Emits a `journal::Kind::ForkOpen` event.
    pub fn spawn_pane(&mut self, label: String, cmd: String, cols: u16, rows: u16) -> anyhow::Result<String> {
        let id   = new_session_id();
        let pane = PtySession::spawn(id.clone(), label, cmd, cols, rows)?;
        self.panes.push(pane);
        journal::emit(journal::Entry {
            kind: journal::Kind::ForkOpen { fork_id: id.clone(), parent: None },
            key: "mux".into(),
            ..Default::default()
        });
        Ok(id)
    }

    /// Look up a pane by session id.
    pub fn find(&self, id: &str) -> Option<&PtySession> {
        self.panes.iter().find(|p| p.id == id)
    }

    /// Mutable look up a pane by session id.
    pub fn find_mut(&mut self, id: &str) -> Option<&mut PtySession> {
        self.panes.iter_mut().find(|p| p.id == id)
    }

    /// Return the currently focused pane.
    pub fn focused(&self) -> Option<&PtySession> {
        self.panes.get(self.focus)
    }

    /// Return a mutable reference to the currently focused pane.
    pub fn focused_mut(&mut self) -> Option<&mut PtySession> {
        self.panes.get_mut(self.focus)
    }

    /// Advance focus to the next pane, wrapping around.
    pub fn cycle_focus(&mut self) {
        if self.panes.len() > 1 {
            self.focus = (self.focus + 1) % self.panes.len();
        }
    }

    /// Drain PTY output into each pane's vt100 parser. Call each frame.
    pub fn drain_all(&mut self) {
        for pane in &mut self.panes {
            pane.drain();
        }
    }

    /// Remove panes whose child process has exited. Emits `ForkClose` for each.
    /// Clamps `focus` to stay in bounds.
    pub fn reap_exited(&mut self) {
        let mut i = 0;
        while i < self.panes.len() {
            if self.panes[i].poll_exited() {
                let id = self.panes[i].id.clone();
                journal::emit(journal::Entry {
                    kind: journal::Kind::ForkClose { fork_id: id },
                    key: "mux".into(),
                    ..Default::default()
                });
                self.panes.remove(i);
                if self.focus >= self.panes.len() && !self.panes.is_empty() {
                    self.focus = self.panes.len() - 1;
                }
            } else {
                i += 1;
            }
        }
        // Ensure at least focus 0 is valid (panes may all be gone).
        if self.panes.is_empty() { self.focus = 0; }
    }

    /// Resize every pane to `(cols, rows)`.
    pub fn resize_all(&mut self, cols: u16, rows: u16) {
        self.cols = cols;
        self.rows = rows;
        for pane in &mut self.panes {
            pane.resize(cols, rows);
        }
    }

    /// Write `text` bytes to the pane identified by `session_id`.
    /// Emits a `Log` journal entry with key `"mux.send"`.
    /// Returns `false` if no pane with that id exists.
    pub fn send_to(&mut self, session_id: &str, text: &str) -> bool {
        let Some(pane) = self.find_mut(session_id) else { return false };
        pane.write_input(text.as_bytes());
        journal::emit(journal::Entry {
            kind: journal::Kind::Log,
            key: "mux.send".into(),
            ..Default::default()
        });
        true
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn make_registry() -> PaneRegistry {
        #[cfg(windows)]
        let cmd = "cmd";
        #[cfg(not(windows))]
        let cmd = "sh";
        PaneRegistry::new(cmd.to_string(), 80, 24).expect("spawn echo pane")
    }

    #[test]
    fn new_creates_main_pane() {
        let reg = make_registry();
        assert_eq!(reg.panes.len(), 1);
        assert_eq!(reg.panes[0].label, "main");
        assert_eq!(reg.focus, 0);
    }

    #[test]
    fn spawn_pane_returns_id_and_appends() {
        let mut reg = make_registry();
        #[cfg(windows)]
        let cmd = "cmd";
        #[cfg(not(windows))]
        let cmd = "sh";
        let id = reg.spawn_pane("sub-1".to_string(), cmd.to_string(), 80, 24).expect("spawn");
        assert_eq!(id.len(), 8);
        assert_eq!(reg.panes.len(), 2);
        assert_eq!(reg.panes[1].label, "sub-1");
    }

    #[test]
    fn cycle_focus_wraps() {
        let mut reg = make_registry();
        #[cfg(windows)]
        let cmd = "cmd";
        #[cfg(not(windows))]
        let cmd = "sh";
        reg.spawn_pane("sub-1".to_string(), cmd.to_string(), 80, 24).unwrap();
        reg.spawn_pane("sub-2".to_string(), cmd.to_string(), 80, 24).unwrap();
        assert_eq!(reg.focus, 0);
        reg.cycle_focus(); assert_eq!(reg.focus, 1);
        reg.cycle_focus(); assert_eq!(reg.focus, 2);
        reg.cycle_focus(); assert_eq!(reg.focus, 0);
    }

    #[test]
    fn find_by_id() {
        let mut reg = make_registry();
        #[cfg(windows)]
        let cmd = "cmd";
        #[cfg(not(windows))]
        let cmd = "sh";
        let id = reg.spawn_pane("sub-1".to_string(), cmd.to_string(), 80, 24).unwrap();
        assert!(reg.find(&id).is_some());
        assert!(reg.find("nonexistent").is_none());
    }
}
```

- [ ] **Step 4: Add re-export to `src/mux/mod.rs`**

Add after the existing `pub use pty::...` line:

```rust
pub use registry::{PaneRegistry, SharedRegistry};
```

- [ ] **Step 5: Run registry tests**

```
cargo test -p kern mux::registry 2>&1 | tail -15
```

Expected: `test result: ok. 4 passed; 0 failed`.

- [ ] **Step 6: Commit**

```
git add src/mux/registry.rs src/mux/mod.rs
git commit -m "feat(mux): add PaneRegistry with spawn/cycle/drain/reap/send"
```

---

## Task 4: TUI Render Loop

**Files:**
- Create: `src/mux/tui.rs`
- Modify: `src/mux/mod.rs` (add re-export)

> **Context:** The TUI uses crossterm raw mode on the host terminal. `Guard` enters and restores it. `draw_frame` renders the two-column split by iterating `vt100::Screen::cell(row, col)` and using `crossterm::queue!(stdout, MoveTo, Print)`. The frame loop polls `crossterm::event::poll(Duration::from_millis(16))` for input. The mux TUI is NOT unit-tested for its render output (crossterm tests are integration-level); we test only the pure functions: `format_tab_strip` and the already-covered `key_event_to_bytes`.

- [ ] **Step 1: Write the failing test for `format_tab_strip`**

Create `src/mux/tui.rs` with just the test:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn format_tab_strip_marks_active_pane() {
        let labels = vec![
            ("id1".to_string(), "main".to_string(), false),
            ("id2".to_string(), "sub-1".to_string(), false),
        ];
        let strip = format_tab_strip(&labels, 0, 60);
        assert!(strip.contains("●main"), "active pane gets bullet: {strip:?}");
        assert!(strip.contains("sub-1"), "inactive pane present: {strip:?}");
        // Active pane bullet, inactive no bullet.
        assert!(!strip.contains("●sub-1"), "inactive pane has no bullet: {strip:?}");
    }

    #[test]
    fn format_tab_strip_shows_exited_as_dead() {
        let labels = vec![
            ("id1".to_string(), "main".to_string(), false),
            ("id2".to_string(), "dead".to_string(), true), // exited
        ];
        let strip = format_tab_strip(&labels, 0, 60);
        assert!(strip.contains("†dead") || strip.contains("dead"), "exited pane shown: {strip:?}");
    }
}
```

- [ ] **Step 2: Run test — verify compile fails**

```
cargo test -p kern mux::tui 2>&1 | head -10
```

- [ ] **Step 3: Implement `src/mux/tui.rs`**

```rust
//! Crossterm TUI render loop for the mux PTY multiplexer.
//!
//! Entry point: `run_tui(registry, keymap)`. Runs until the user presses
//! the quit key or all panes exit. The terminal is restored on normal exit
//! and on panic via `Guard`.

use std::io::{self, Write};
use std::time::Duration;

use crossterm::{
    cursor::{Hide, MoveTo, Show},
    event::{self, Event, KeyEventKind},
    execute, queue,
    style::{Color, Print, ResetColor, SetForegroundColor},
    terminal::{self, Clear, ClearType, EnterAlternateScreen, LeaveAlternateScreen},
};

use crate::config::KeyMap;
use crate::mux::pty::key_event_to_bytes;
use crate::mux::registry::PaneRegistry;

// ── Terminal guard ────────────────────────────────────────────────────────────

/// Enters raw mode + alt-screen on construction; restores on `Drop`.
pub struct Guard;

impl Guard {
    pub fn enter() -> io::Result<Self> {
        #[cfg(windows)]
        enable_vt_windows()?;
        terminal::enable_raw_mode()?;
        execute!(io::stdout(), EnterAlternateScreen, Hide)?;
        Ok(Guard)
    }
}

impl Drop for Guard {
    fn drop(&mut self) {
        let _ = execute!(io::stdout(), Show, LeaveAlternateScreen);
        let _ = terminal::disable_raw_mode();
    }
}

/// Install a panic hook that restores the terminal before printing the panic.
pub fn install_panic_hook() {
    let default = std::panic::take_hook();
    std::panic::set_hook(Box::new(move |info| {
        let _ = execute!(io::stdout(), Show, LeaveAlternateScreen);
        let _ = terminal::disable_raw_mode();
        default(info);
    }));
}

#[cfg(windows)]
fn enable_vt_windows() -> io::Result<()> {
    use std::os::windows::io::AsRawHandle;
    // ENABLE_VIRTUAL_TERMINAL_PROCESSING = 0x0004
    unsafe {
        let h = io::stdout().as_raw_handle() as isize;
        let mut mode: u32 = 0;
        if windows_sys::Win32::System::Console::GetConsoleMode(h as _, &mut mode) == 0 {
            return Err(io::Error::last_os_error());
        }
        if windows_sys::Win32::System::Console::SetConsoleMode(h as _, mode | 0x0004) == 0 {
            return Err(io::Error::last_os_error());
        }
    }
    Ok(())
}

// ── Main TUI loop ─────────────────────────────────────────────────────────────

/// Run the mux TUI until the user quits or all panes exit.
pub fn run_tui(registry: &mut PaneRegistry, keymap: &KeyMap) -> io::Result<()> {
    install_panic_hook();
    let _guard = Guard::enter()?;
    let (mut cols, mut rows) = terminal::size()?;

    let mut stdout = io::stdout();
    queue!(stdout, Clear(ClearType::All))?;
    stdout.flush()?;

    loop {
        // Drain PTY output into vt100 parsers.
        registry.drain_all();
        registry.reap_exited();

        // Exit if all panes are gone.
        if registry.panes.is_empty() {
            break;
        }

        // Draw this frame.
        draw_frame(registry, &mut stdout, cols, rows)?;
        stdout.flush()?;

        // Poll for crossterm events with a 16 ms timeout (~60 Hz).
        if event::poll(Duration::from_millis(16))? {
            match event::read()? {
                Event::Resize(w, h) => {
                    cols = w;
                    rows = h;
                    registry.resize_all(cols, rows.saturating_sub(1));
                    queue!(stdout, Clear(ClearType::All))?;
                }
                Event::Key(kev) if kev.kind == KeyEventKind::Press => {
                    if keymap.matches_quit(&kev) {
                        break;
                    } else if keymap.matches_cycle(&kev) {
                        registry.cycle_focus();
                    } else if keymap.matches_new_pane(&kev) {
                        // Spawn a new sub-pane. Label is "sub-N" where N = pane count.
                        let n = registry.panes.len();
                        let cmd = registry.panes[0].cmd.clone();
                        let _ = registry.spawn_pane(
                            format!("sub-{n}"),
                            cmd,
                            cols / 2,
                            rows.saturating_sub(1),
                        );
                    } else if keymap.matches_close_pane(&kev) {
                        // Close focused pane; never close main (index 0).
                        if registry.focus > 0 {
                            if let Some(p) = registry.focused_mut() {
                                p.kill();
                            }
                        }
                    } else if let Some(bytes) = key_event_to_bytes(&kev) {
                        // Forward keystroke to focused pane.
                        if let Some(p) = registry.focused_mut() {
                            p.write_input(&bytes);
                        }
                    }
                }
                _ => {}
            }
        }
    }

    Ok(())
}

// ── Rendering ─────────────────────────────────────────────────────────────────

/// Render one frame: two-column split + one-row tab strip.
///
/// ```text
/// ┌────────────────┬───────────────┐
/// │  Main (left)   │  Focus (right)│  rows - 1
/// ├────────────────┴───────────────┤
/// │ [●main][sub-1]      kern 12:34 │  1 row
/// └────────────────────────────────┘
/// ```
pub fn draw_frame(
    registry: &PaneRegistry,
    stdout: &mut impl Write,
    cols: u16,
    rows: u16,
) -> io::Result<()> {
    let pane_rows = rows.saturating_sub(1);
    let left_cols  = cols / 2;
    let right_cols = cols - left_cols;

    // ── Left pane (always panes[0] = main) ───────────────────────────────
    if let Some(main) = registry.panes.get(0) {
        draw_pane(stdout, main.parser.screen(), 0, left_cols, pane_rows)?;
    }

    // ── Vertical divider ─────────────────────────────────────────────────
    for row in 0..pane_rows {
        queue!(stdout, MoveTo(left_cols, row), Print("│"))?;
    }

    // ── Right pane (focused, if not main) ────────────────────────────────
    if registry.focus > 0 {
        if let Some(sub) = registry.focused() {
            draw_pane(stdout, sub.parser.screen(), left_cols + 1, right_cols.saturating_sub(1), pane_rows)?;
        }
    }

    // ── Tab strip ────────────────────────────────────────────────────────
    let labels: Vec<(String, String, bool)> = registry
        .panes
        .iter()
        .map(|p| (p.id.clone(), p.label.clone(), p.exited))
        .collect();
    let strip = format_tab_strip(&labels, registry.focus, cols as usize);
    queue!(
        stdout,
        MoveTo(0, pane_rows),
        SetForegroundColor(Color::DarkGrey),
        Print(&strip),
        ResetColor,
    )?;

    Ok(())
}

/// Render a `vt100::Screen` into a rectangular terminal region.
/// Uses plain text only (no colour attributes) for simplicity.
fn draw_pane(
    stdout: &mut impl Write,
    screen: &vt100::Screen,
    col_offset: u16,
    width: u16,
    height: u16,
) -> io::Result<()> {
    let (screen_rows, screen_cols) = screen.size();
    for row in 0..height {
        queue!(stdout, MoveTo(col_offset, row))?;
        let mut line = String::with_capacity(width as usize);
        for col in 0..width {
            if col < screen_cols && row < screen_rows {
                let c = screen.cell(row, col).map(|c| c.contents().to_string()).unwrap_or_default();
                if c.is_empty() { line.push(' '); } else { line.push_str(&c); }
            } else {
                line.push(' ');
            }
        }
        queue!(stdout, Print(&line))?;
    }
    Ok(())
}

/// Build the one-row tab strip string.
/// `pane_list` is `(session_id, label, exited)`.
pub fn format_tab_strip(
    pane_list: &[(String, String, bool)],
    focus: usize,
    total_cols: usize,
) -> String {
    let mut left = String::new();
    for (i, (_id, label, exited)) in pane_list.iter().enumerate() {
        let marker = if i == focus { "●" } else { " " };
        let display = if *exited {
            format!(" {marker}†{label} ")
        } else {
            format!(" {marker}{label} ")
        };
        left.push_str(&display);
    }
    let right = format!(" kern ");
    let padding = total_cols.saturating_sub(left.len() + right.len());
    format!("{left}{}{right}", " ".repeat(padding))
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn format_tab_strip_marks_active_pane() {
        let labels = vec![
            ("id1".to_string(), "main".to_string(),  false),
            ("id2".to_string(), "sub-1".to_string(), false),
        ];
        let strip = format_tab_strip(&labels, 0, 60);
        assert!(strip.contains("●main"),  "active pane bullet: {strip:?}");
        assert!(strip.contains("sub-1"),  "inactive pane present: {strip:?}");
        assert!(!strip.contains("●sub-1"), "inactive has no bullet: {strip:?}");
    }

    #[test]
    fn format_tab_strip_shows_exited_as_dead() {
        let labels = vec![
            ("id1".to_string(), "main".to_string(), false),
            ("id2".to_string(), "dead".to_string(), true),
        ];
        let strip = format_tab_strip(&labels, 0, 60);
        assert!(strip.contains("†dead"), "exited pane shown with dagger: {strip:?}");
    }
}
```

> **Windows note:** `enable_vt_windows` uses `windows_sys`. Add to `kern/Cargo.toml` under `[target.'cfg(windows)'.dependencies]` (create the section if missing):
> ```toml
> [target.'cfg(windows)'.dependencies]
> windows-sys = { version = "0.61", features = ["Win32_System_Console", "Win32_Foundation"] }
> ```

- [ ] **Step 4: Run tui tests**

```
cargo test -p kern mux::tui 2>&1 | tail -15
```

Expected: `test result: ok. 2 passed; 0 failed`.

- [ ] **Step 5: Commit**

```
git add src/mux/tui.rs Cargo.toml Cargo.lock
git commit -m "feat(mux): add TUI render loop, Guard, draw_frame, format_tab_strip"
```

---

## Task 5: MCP Tools

**Files:**
- Create: `src/mux/mcp.rs`
- Modify: `src/mux/mod.rs` (add re-export)

> **Context:** `MuxMcpServer` implements `trnsprt::McpServer` (from `shared/trnsprt`). It holds a `SharedRegistry` (`Arc<Mutex<PaneRegistry>>`). Tools follow the same schema+handler co-location pattern as `src/mcp/tools_query.rs`. The MCP server is served over TCP via `trnsprt::serve_rw` (which takes any `BufRead + Write`) — one `std::thread` per accepted connection. The TCP listener is started in `run_mux` (Task 6); this task only defines the server struct and tools.

- [ ] **Step 1: Write the failing tests**

Create `src/mux/mcp.rs` with just tests:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn tool_definitions_are_well_formed() {
        let defs = tool_schemas();
        let names: Vec<&str> = defs.iter()
            .map(|d| d["name"].as_str().expect("tool has name"))
            .collect();
        assert_eq!(names, ["mux_spawn", "mux_send", "mux_list", "mux_status"]);
        for d in &defs {
            let name = d["name"].as_str().unwrap();
            assert!(d["inputSchema"].is_object(), "{name}: inputSchema must be object");
            assert_eq!(d["inputSchema"]["type"], "object", "{name}: type must be object");
        }
    }

    #[test]
    fn mux_spawn_schema_requires_label() {
        let defs = tool_schemas();
        let spawn = defs.iter().find(|d| d["name"] == "mux_spawn").unwrap();
        let required = spawn["inputSchema"]["required"].as_array().unwrap();
        let req_strs: Vec<&str> = required.iter().filter_map(|v| v.as_str()).collect();
        assert!(req_strs.contains(&"label"), "mux_spawn must require label");
    }

    #[test]
    fn mux_send_schema_requires_session_id_and_text() {
        let defs = tool_schemas();
        let send = defs.iter().find(|d| d["name"] == "mux_send").unwrap();
        let required = send["inputSchema"]["required"].as_array().unwrap();
        let req_strs: Vec<&str> = required.iter().filter_map(|v| v.as_str()).collect();
        assert!(req_strs.contains(&"session_id"), "mux_send requires session_id");
        assert!(req_strs.contains(&"text"),       "mux_send requires text");
    }

    #[test]
    fn mux_status_schema_requires_session_id() {
        let defs = tool_schemas();
        let status = defs.iter().find(|d| d["name"] == "mux_status").unwrap();
        let required = status["inputSchema"]["required"].as_array().unwrap();
        let req_strs: Vec<&str> = required.iter().filter_map(|v| v.as_str()).collect();
        assert!(req_strs.contains(&"session_id"), "mux_status requires session_id");
    }
}
```

- [ ] **Step 2: Run test — verify compile fails**

```
cargo test -p kern mux::mcp 2>&1 | head -10
```

- [ ] **Step 3: Implement `src/mux/mcp.rs`**

```rust
//! MCP server for the mux PTY multiplexer.
//!
//! Exposes four tools (mux_spawn, mux_send, mux_list, mux_status) that
//! agents running inside panes can call to manage sibling panes.
//! Only active in mux mode — never registered when running `--daemon`.
//!
//! Transport: TCP loopback, one thread per accepted connection (see run_mux).

use std::sync::{Arc, Mutex};

use serde::Deserialize;
use trnsprt::{McpError, McpServer, ToolResult, ToolSchema};

use crate::mux::registry::PaneRegistry;

pub struct MuxMcpServer {
    pub registry: Arc<Mutex<PaneRegistry>>,
    pub agent_cmd: String,
}

// ── Tool schemas ──────────────────────────────────────────────────────────────

pub fn tool_schemas() -> Vec<serde_json::Value> {
    vec![
        serde_json::json!({
            "name": "mux_spawn",
            "description": "Spawn a new agent sub-pane in the mux TUI.",
            "inputSchema": {
                "type": "object",
                "required": ["label"],
                "properties": {
                    "label": { "type": "string", "description": "Human label for the new pane (e.g. 'worker-1')" },
                    "cmd":   { "type": "string", "description": "Command to run (defaults to configured agent_cmd)" },
                },
            },
        }),
        serde_json::json!({
            "name": "mux_send",
            "description": "Write text to a pane's PTY stdin.",
            "inputSchema": {
                "type": "object",
                "required": ["session_id", "text"],
                "properties": {
                    "session_id": { "type": "string", "description": "Session id from mux_spawn or mux_list" },
                    "text":       { "type": "string", "description": "Text to write to the pane's PTY stdin" },
                },
            },
        }),
        serde_json::json!({
            "name": "mux_list",
            "description": "List all active panes.",
            "inputSchema": {
                "type": "object",
                "properties": {},
            },
        }),
        serde_json::json!({
            "name": "mux_status",
            "description": "Get the current visible screen content of a pane.",
            "inputSchema": {
                "type": "object",
                "required": ["session_id"],
                "properties": {
                    "session_id": { "type": "string", "description": "Session id of the pane to inspect" },
                },
            },
        }),
    ]
}

// ── McpServer impl ────────────────────────────────────────────────────────────

impl McpServer for MuxMcpServer {
    fn server_name(&self)    -> &str { "kern-mux" }
    fn server_version(&self) -> &str { env!("CARGO_PKG_VERSION") }

    fn tools_list(&self) -> Vec<ToolSchema> {
        tool_schemas()
            .into_iter()
            .filter_map(|v| serde_json::from_value(v).ok())
            .collect()
    }

    fn call_tool(&self, name: &str, args: &serde_json::Value) -> Result<ToolResult, McpError> {
        let result = match name {
            "mux_spawn"  => self.tool_spawn(args),
            "mux_send"   => self.tool_send(args),
            "mux_list"   => self.tool_list(),
            "mux_status" => self.tool_status(args),
            _            => tool_error(&format!("unknown mux tool: {name}")),
        };
        Ok(value_to_tool_result(result))
    }
}

// ── Tool handlers ─────────────────────────────────────────────────────────────

#[derive(Deserialize)]
struct SpawnArgs { label: String, cmd: Option<String> }

#[derive(Deserialize)]
struct SendArgs { session_id: String, text: String }

#[derive(Deserialize)]
struct IdArgs { session_id: String }

impl MuxMcpServer {
    fn tool_spawn(&self, args: &serde_json::Value) -> serde_json::Value {
        let p: SpawnArgs = match serde_json::from_value(args.clone()) {
            Ok(v) => v,
            Err(e) => return tool_error(&format!("invalid args: {e}")),
        };
        let cmd = p.cmd.unwrap_or_else(|| self.agent_cmd.clone());
        let mut reg = match self.registry.lock() {
            Ok(g) => g,
            Err(_) => return tool_error("registry lock poisoned"),
        };
        let cols = reg.cols;
        let rows = reg.rows;
        match reg.spawn_pane(p.label, cmd, cols, rows) {
            Ok(id)  => tool_ok(serde_json::json!({ "session_id": id })),
            Err(e)  => tool_error(&format!("spawn failed: {e}")),
        }
    }

    fn tool_send(&self, args: &serde_json::Value) -> serde_json::Value {
        let p: SendArgs = match serde_json::from_value(args.clone()) {
            Ok(v) => v,
            Err(e) => return tool_error(&format!("invalid args: {e}")),
        };
        let mut reg = match self.registry.lock() {
            Ok(g) => g,
            Err(_) => return tool_error("registry lock poisoned"),
        };
        if reg.send_to(&p.session_id, &p.text) {
            tool_ok(serde_json::json!({}))
        } else {
            tool_error(&format!("no pane with id: {}", p.session_id))
        }
    }

    fn tool_list(&self) -> serde_json::Value {
        let reg = match self.registry.lock() {
            Ok(g) => g,
            Err(_) => return tool_error("registry lock poisoned"),
        };
        let panes: Vec<serde_json::Value> = reg.panes.iter().map(|p| {
            serde_json::json!({
                "session_id": p.id,
                "label":      p.label,
                "exited":     p.exited,
            })
        }).collect();
        tool_ok(serde_json::json!(panes))
    }

    fn tool_status(&self, args: &serde_json::Value) -> serde_json::Value {
        let p: IdArgs = match serde_json::from_value(args.clone()) {
            Ok(v) => v,
            Err(e) => return tool_error(&format!("invalid args: {e}")),
        };
        let reg = match self.registry.lock() {
            Ok(g) => g,
            Err(_) => return tool_error("registry lock poisoned"),
        };
        let Some(pane) = reg.find(&p.session_id) else {
            return tool_error(&format!("no pane with id: {}", p.session_id));
        };
        let screen = pane.parser.screen();
        let (rows, cols) = screen.size();
        let (cursor_row, cursor_col) = screen.cursor_position();
        let screen_text = pane.screen_text();
        tool_ok(serde_json::json!({
            "session_id":  pane.id,
            "label":       pane.label,
            "exited":      pane.exited,
            "cols":        cols,
            "rows":        rows,
            "cursor_row":  cursor_row,
            "cursor_col":  cursor_col,
            "screen_text": screen_text,
        }))
    }
}

// ── Helpers ───────────────────────────────────────────────────────────────────

fn tool_ok(v: serde_json::Value) -> serde_json::Value {
    let s = serde_json::to_string(&v).unwrap_or_default();
    serde_json::json!({ "content": [{ "type": "text", "text": s }] })
}

fn tool_error(msg: &str) -> serde_json::Value {
    serde_json::json!({
        "isError": true,
        "content": [{ "type": "text", "text": msg }],
    })
}

fn value_to_tool_result(v: serde_json::Value) -> ToolResult {
    let is_error = v.get("isError").and_then(|v| v.as_bool()).unwrap_or(false);
    let content  = v.get("content").and_then(|v| v.as_array()).cloned().unwrap_or_default();
    ToolResult { content, is_error, structured_content: None }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn tool_definitions_are_well_formed() {
        let defs  = tool_schemas();
        let names: Vec<&str> = defs.iter()
            .map(|d| d["name"].as_str().expect("name"))
            .collect();
        assert_eq!(names, ["mux_spawn", "mux_send", "mux_list", "mux_status"]);
        for d in &defs {
            let name = d["name"].as_str().unwrap();
            assert!(d["inputSchema"].is_object(), "{name}: needs inputSchema");
            assert_eq!(d["inputSchema"]["type"], "object");
        }
    }

    #[test]
    fn mux_spawn_schema_requires_label() {
        let defs   = tool_schemas();
        let spawn  = defs.iter().find(|d| d["name"] == "mux_spawn").unwrap();
        let req    = spawn["inputSchema"]["required"].as_array().unwrap();
        let strs: Vec<&str> = req.iter().filter_map(|v| v.as_str()).collect();
        assert!(strs.contains(&"label"));
    }

    #[test]
    fn mux_send_schema_requires_session_id_and_text() {
        let defs  = tool_schemas();
        let send  = defs.iter().find(|d| d["name"] == "mux_send").unwrap();
        let req   = send["inputSchema"]["required"].as_array().unwrap();
        let strs: Vec<&str> = req.iter().filter_map(|v| v.as_str()).collect();
        assert!(strs.contains(&"session_id"));
        assert!(strs.contains(&"text"));
    }

    #[test]
    fn mux_status_schema_requires_session_id() {
        let defs   = tool_schemas();
        let status = defs.iter().find(|d| d["name"] == "mux_status").unwrap();
        let req    = status["inputSchema"]["required"].as_array().unwrap();
        let strs: Vec<&str> = req.iter().filter_map(|v| v.as_str()).collect();
        assert!(strs.contains(&"session_id"));
    }
}
```

- [ ] **Step 4: Run MCP tests**

```
cargo test -p kern mux::mcp 2>&1 | tail -15
```

Expected: `test result: ok. 4 passed; 0 failed`.

- [ ] **Step 5: Commit**

```
git add src/mux/mcp.rs
git commit -m "feat(mux): add MuxMcpServer with mux_spawn/send/list/status tools"
```

---

## Task 6: Entry Point Wiring

**Files:**
- Modify: `src/mux/mod.rs` (replace stub with full `run_mux`)
- Modify: `src/commands.rs` (add `Commands::Daemon`, dispatch arm)
- Modify: `src/main.rs` (update dispatch match)

- [ ] **Step 1: Write the entry-point routing test**

In `src/commands.rs`, the test verifies that `Commands::Daemon` exists as a variant. Add a test at the bottom of `commands.rs`:

```rust
#[cfg(test)]
mod entry_point_tests {
    use super::Commands;

    #[test]
    fn daemon_subcommand_exists() {
        // This test simply confirms Commands::Daemon compiles —
        // a regression guard against accidental removal.
        let _ = Commands::Daemon;
    }
}
```

- [ ] **Step 2: Run test — verify compile fails (Commands::Daemon not defined)**

```
cargo test -p kern entry_point_tests 2>&1 | head -10
```

- [ ] **Step 3: Add `Commands::Daemon` to `src/commands.rs`**

In the `Commands` enum (after the existing `Hunt` variant or at the end), add:

```rust
/// Start the long-lived daemon (same as `--daemon`). Convenience alias.
Daemon,
```

Add the dispatch arm in `pub async fn dispatch(...)` at the end of the `match cmd` block, just before the closing brace:

```rust
Commands::Daemon => {
    // Dispatch always goes through commands.rs where Cli is available;
    // use a daemon-defaults Cli to call run_server.
    let default_cli = Cli {
        command:     None,
        daemon:      true,
        mcp_addr:    String::new(),
        mcp_stdio:   false,
        embed_url:   crate::config::DEFAULT_EMBED_URL.to_string(),
        embed_model: crate::config::DEFAULT_EMBED_MODEL.to_string(),
        reason_url:  String::new(),
        reason_model: String::new(),
    };
    run_server(&default_cli, cfg).await;
}
```

- [ ] **Step 4: Run entry-point test — verify it now passes**

```
cargo test -p kern entry_point_tests 2>&1 | tail -10
```

Expected: `test result: ok. 1 passed; 0 failed`.

- [ ] **Step 5: Replace stub `run_mux` in `src/mux/mod.rs`**

Replace the entire file with the full implementation:

```rust
//! Mux PTY-multiplexer TUI — default `kern` launch mode.
//!
//! `run_mux` starts the TUI and a background MCP server task that exposes
//! the four `mux_*` tools on a TCP loopback socket.

pub mod mcp;
pub mod pty;
pub mod registry;
pub mod tui;

pub use mcp::MuxMcpServer;
pub use pty::{new_session_id, PtySession};
pub use registry::{PaneRegistry, SharedRegistry};
pub use tui::run_tui;

use std::sync::{Arc, Mutex};

use crate::config::{Config, KeyMap};

/// Launch the mux TUI.
///
/// 1. Starts the MCP server on `cfg.mux.mcp_addr` in a background task.
/// 2. Runs the TUI render loop (blocks until quit).
/// 3. Cancels the MCP task on return.
pub async fn run_mux(cfg: &Config) {
    // Determine terminal size (fall back to 80×24 if detection fails).
    let (cols, rows) = crossterm::terminal::size().unwrap_or((80, 24));
    let pane_rows    = rows.saturating_sub(1);

    let registry = match PaneRegistry::new(cfg.mux.agent_cmd.clone(), cols / 2, pane_rows) {
        Ok(r)  => Arc::new(Mutex::new(r)),
        Err(e) => {
            eprintln!("kern mux: failed to spawn main pane: {e}");
            return;
        }
    };

    // ── MCP server ────────────────────────────────────────────────────────
    let mcp_addr   = cfg.mux.mcp_addr.clone();
    let agent_cmd  = cfg.mux.agent_cmd.clone();
    let reg_mcp    = registry.clone();
    let (cancel_tx, cancel_rx) = tokio::sync::oneshot::channel::<()>();

    tokio::spawn(async move {
        let listener = match tokio::net::TcpListener::bind(&mcp_addr).await {
            Ok(l) => l,
            Err(e) => {
                tracing::warn!(target: "kern.mux", addr = %mcp_addr, error = %e, "mux MCP bind failed");
                return;
            }
        };
        tracing::info!(target: "kern.mux", addr = %mcp_addr, "mux MCP listening");

        let mut cancel_rx = cancel_rx;
        loop {
            tokio::select! {
                _ = &mut cancel_rx => break,
                result = listener.accept() => {
                    let Ok((stream, _)) = result else { continue };
                    let Ok(std_stream) = stream.into_std() else { continue };
                    let reg  = reg_mcp.clone();
                    let cmd  = agent_cmd.clone();
                    std::thread::spawn(move || {
                        let server = MuxMcpServer { registry: reg, agent_cmd: cmd };
                        let reader_stream = match std_stream.try_clone() {
                            Ok(s) => s,
                            Err(_) => return,
                        };
                        let mut reader = std::io::BufReader::new(reader_stream);
                        let mut writer = std_stream;
                        let _ = trnsprt::serve_rw(&mut reader, &mut writer, &server);
                    });
                }
            }
        }
    });

    // ── TUI loop (blocking) ───────────────────────────────────────────────
    let keymap = KeyMap::from_config(&cfg.mux);
    {
        let mut reg = registry.lock().expect("registry lock");
        if let Err(e) = run_tui(&mut reg, &keymap) {
            eprintln!("kern mux: TUI error: {e}");
        }
    }

    // Signal the MCP task to stop.
    let _ = cancel_tx.send(());
}
```

- [ ] **Step 6: Update `src/main.rs`**

Change the imports at the top from:
```rust
use kern::commands::{Cli, dispatch, run_server};
```
to:
```rust
use kern::commands::{Cli, Commands, dispatch, run_server};
use kern::mux::run_mux;
```

Change the dispatch block inside `rt.block_on(async { ... })` from:
```rust
match cli.command {
    Some(cmd) => dispatch(cmd, &cfg).await,
    None => run_server(&cli, &cfg).await,
}
```
to:
```rust
match cli.command {
    Some(Commands::Daemon) => run_server(&cli, &cfg).await,
    Some(cmd)              => dispatch(cmd, &cfg).await,
    None if cli.daemon     => run_server(&cli, &cfg).await,
    None                   => run_mux(&cfg).await,
}
```

- [ ] **Step 7: Build the full crate**

```
cargo build -p kern 2>&1 | tail -20
```

Expected: `Finished` — no errors. Fix any warnings about unused fields only if they are errors (repo has `warnings = "deny"`).

- [ ] **Step 8: Run all tests**

```
cargo test -p kern 2>&1 | tail -20
```

Expected: all existing tests pass; new mux tests pass. No regressions.

- [ ] **Step 9: Smoke-test the daemon path still works**

```
cargo run -p kern -- daemon --help 2>&1 | head -5
```

Expected: clap help output for `kern daemon` subcommand (no panic).

- [ ] **Step 10: Commit**

```
git add src/mux/mod.rs src/commands.rs src/main.rs
git commit -m "feat(mux): wire run_mux as default entry point; add Commands::Daemon alias"
```

---

## Self-Review Checklist (run before marking plan complete)

- [ ] Spec section "Entry Point" covered by Task 6 ✓
- [ ] Spec section "PtySession" covered by Task 2 ✓
- [ ] Spec section "PaneRegistry" covered by Task 3 ✓
- [ ] Spec section "TUI Render Loop" covered by Task 4 ✓
- [ ] Spec section "MuxMcpServer + 4 tools" covered by Task 5 ✓
- [ ] Spec section "MuxConfig + KeyMap" covered by Task 1 ✓
- [ ] `portable-pty` and `vt100` deps added in Task 1 ✓
- [ ] `windows-sys` dep added in Task 4 ✓
- [ ] Journal events (`ForkOpen`, `ForkClose`, `Log`) emitted in Task 3 ✓
- [ ] No "relay" or "replicator" names anywhere in new code ✓
- [ ] Version stays `1.0.0` (no Cargo.toml version bump) ✓
